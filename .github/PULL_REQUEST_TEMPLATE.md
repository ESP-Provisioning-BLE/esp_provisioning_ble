## Summary

<!-- What does this change do, and why? -->

<!--
Link the issue this addresses:
  "Closes #123"  to close it automatically on merge, or
  "Part of #123" if it only partially advances the issue (avoid close/fix/resolve so it is not auto-closed).
-->

## Checklist

- [ ] `dart format lib test` reports no changes.
- [ ] `flutter analyze` is clean.
- [ ] `flutter test` passes (run with `--coverage`; non-generated coverage stays at or above 60%).
- [ ] `dart pub publish --dry-run` is clean (for changes affecting the published package).
- [ ] Added a `CHANGELOG.md` entry under `## [Unreleased]`.
- [ ] Commits follow Conventional Commits (signing is encouraged but not required).
- [ ] Linked the issue this pull request addresses.
