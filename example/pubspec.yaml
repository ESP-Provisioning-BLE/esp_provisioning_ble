name: example
description: ESP32 BLE provisioning example using flutter_blue_plus

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  esp_provisioning_ble:
    path: ../
  flutter_blue_plus: ^1.32.0
  permission_handler: ^11.0.0
  logger: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
